<Window x:Class="DngOpcodesEditor.MainWindow" xmlns:local="clr-namespace:DngOpcodesEditor"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" mc:Ignorable="d" 
        WindowStartupLocation="CenterScreen" WindowState="Normal" SnapsToDevicePixels="True" AllowDrop="True" Drop="Window_Drop"
        Title="" Width="1200" Height="768" Background="#FFDEDEDE">
    <Window.DataContext>
        <local:MainWindowVM x:Name="ViewModel" />
    </Window.DataContext>
    <Grid Margin="10">
        <Grid.Resources>
            <!--MouseMove="Image_MouseMove" MouseLeave="Image_MouseLeave"-->
            <Style TargetType="Image">
                <EventSetter Event="MouseMove" Handler="Image_MouseMove" />
                <EventSetter Event="MouseLeave" Handler="Image_MouseLeave" />
            </Style>
        </Grid.Resources>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="10"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="220" />
            <RowDefinition Height="10"/>
            <RowDefinition/>
            <RowDefinition Height="5"/>
            <RowDefinition Height="Auto"/>
            <!-- Options (checkboxes) on their own row… -->
            <RowDefinition Height="5"/>
            <RowDefinition Height="Auto"/>
            <!-- …then the mouse-position / pixel-value text, so the two don't collide. -->
            <RowDefinition Height="5"/>
            <RowDefinition Height="30"/>
        </Grid.RowDefinitions>
        <TabControl Grid.Column="2">
            <TabItem Header="Parameters">
                <DataGrid ItemsSource="{Binding SelectedOpcode.Parameters}" AutoGenerateColumns="False"
                          CanUserAddRows="False" CanUserDeleteRows="False">
                    <DataGrid.Columns>
                        <DataGridTextColumn IsReadOnly="True" Header="Parameter" Binding="{Binding Description}" />
                        <DataGridTextColumn Header="Value" Binding="{Binding Value, StringFormat=F4}" />
                        <DataGridTemplateColumn Header="Value (Slider)" Width="*">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Slider Value="{Binding Value, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                            Minimum="-10" Maximum="10" SmallChange="0.01" LargeChange="0.1"
                                            TickFrequency="0.01" IsSnapToTickEnabled="True"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </TabItem>
            <TabItem Header="Metadata">
                <DataGrid ItemsSource="{Binding Metadata}" AutoGenerateColumns="False"
                          CanUserAddRows="False" CanUserDeleteRows="False" IsReadOnly="True">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Tag" Binding="{Binding Name}" />
                        <DataGridTextColumn Header="Value" Binding="{Binding Value}" Width="*" />
                    </DataGrid.Columns>
                </DataGrid>
            </TabItem>
        </TabControl>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="3*"/>
                <ColumnDefinition Width="10"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <ListView ItemsSource="{Binding Opcodes}" SelectedItem="{Binding SelectedOpcode}">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="Enabled">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <CheckBox IsChecked="{Binding Enabled}" />
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                        <GridViewColumn Header="OpcodeID" DisplayMemberBinding="{Binding}" />
                        <GridViewColumn Header="OpcodeList" DisplayMemberBinding="{Binding ListIndex}" />
                    </GridView>
                </ListView.View>
            </ListView>
            <!--
                Right-side action column laid out as a 2-column grid so the
                button stack is roughly half as tall as a plain vertical list.
                Pairing: import-on-left / export-on-right — reading across each
                row tells you what file format is being moved.
            -->
            <Grid Grid.Column="2">
                <Grid.Resources>
                    <Style TargetType="Button">
                        <Setter Property="Margin" Value="0,0,0,10" />
                        <Setter Property="Padding" Value="5" />
                    </Style>
                </Grid.Resources>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="5"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Button Grid.Row="0" Grid.ColumnSpan="3" Content="Open DNG (image + opcodes)"
                        Command="{Binding OpenDngWithOpcodesCommand}" FontWeight="Bold" />
                <Button Grid.Row="1" Grid.Column="0" Content="Import from DNG"    Command="{Binding ImportDngCommand}" />
                <Button Grid.Row="1" Grid.Column="2" Content="Export to DNG"      Command="{Binding ExportDngCommand}" />
                <Button Grid.Row="2" Grid.Column="0" Content="Import from Binary" Command="{Binding ImportBinCommand}" />
                <Button Grid.Row="2" Grid.Column="2" Content="Export to Binary"   Command="{Binding ExportBinCommand}" />
                <Button Grid.Row="3" Grid.Column="0" Content="Delete Opcode"      Command="{Binding DeleteOpcodeCommand}" />
                <Button Grid.Row="3" Grid.Column="2" Content="Clear"              Command="{Binding ClearCommand}" />
                <ComboBox Grid.Row="4" Grid.Column="0" Margin="0,0,0,10"
                          ItemsSource="{Binding OpcodeIds}" SelectedItem="{Binding SelectedOpcodeId}" />
                <Button Grid.Row="4" Grid.Column="2" Content="Add Opcode"
                        Command="{Binding AddSelectedOpcodeCommand}" />
            </Grid>
        </Grid>
        <Image x:Name="imageSrc" Grid.Row="2" Source="{Binding ImgSrc.Bmp}" />
        <Button Content="Open Reference Image" Grid.Row="8" Command="{Binding OpenImageCommand}" />
        <Image x:Name="imageDst" Grid.Row="2" Source="{Binding ImgDst.Bmp}" Grid.Column="2"/>
        <Button Content="Save Preview Image" Grid.Row="8" Grid.Column="2" Command="{Binding SaveImageCommand}" />
        <GridSplitter Grid.ColumnSpan="3" Grid.Row="1" HorizontalAlignment="Stretch" Background="#00F0F0F0" />
        <TextBlock x:Name="tbInfo" Grid.ColumnSpan="3" Grid.Row="6" HorizontalAlignment="Center" d:Text="Info" />
        <TextBlock x:Name="tbPosition" Grid.ColumnSpan="3" Grid.Row="6" HorizontalAlignment="Right" d:Text="Position" />

        <StackPanel Grid.Row="4" Grid.ColumnSpan="3" Orientation="Horizontal">
            <CheckBox IsChecked="{Binding DecodeGamma,Mode=TwoWay}" Content="Decode Input (sRGB)"
                      ToolTip="Decode gamma-encoded inputs (typically 8-bit TIFF / PNG) with the sRGB EOTF before applying opcodes." />
            <CheckBox IsChecked="{Binding EncodeGamma,Mode=TwoWay}" Content="Encode Output (sRGB)" Margin="5,0,0,0"
                      ToolTip="Encode the preview with the sRGB OETF so it displays correctly on a standard sRGB monitor." />
            <CheckBox IsChecked="{Binding ApplyColorTransform,Mode=TwoWay}" Content="DNG Color Transform" Margin="20,0,0,0"
                      ToolTip="White balance + ColorMatrix from the DNG (AsShotNeutral and ColorMatrix2)" />
            <CheckBox IsChecked="{Binding ProcessAtFullResolution,Mode=TwoWay}" Content="Process at full resolution" Margin="20,0,0,0"
                      ToolTip="By default the preview is downsampled to FHD (1920x1080) for responsiveness on large DNGs" />
            <CheckBox IsChecked="{Binding DitherDisplay,Mode=TwoWay}" Content="Display Dither" Margin="20,0,0,0"
                      ToolTip="TPDF dither at the 16→8 bit display conversion. Live preview only — saved TIFFs are 16-bit and untouched. Toggle off to A/B vs. the raw truncated 8-bit." />
        </StackPanel>
        
    </Grid>
</Window>